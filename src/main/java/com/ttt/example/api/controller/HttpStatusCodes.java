package com.ttt.example.api.controller;

import org.apache.http.HttpStatus;

/**
 * Including constants for HTTP Status codes.
 * Adding custom class to avoid name collision with import org.springframework.http.HttpStatus
 */
public class HttpStatusCodes implements HttpStatus
{
}
